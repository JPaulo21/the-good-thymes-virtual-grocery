<?xml version="1.0" encoding="UTF-8" ?>
<configuration>
    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{dd/MM/yyyy HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="file" class="ch.qos.logback.core.FileAppender">
        <file>src/main/resources/logs/app.log</file>
        <append>true</append>
        <encoder>
            <pattern>%d{dd/MM/yyyy HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

<!--    <appender name="socket" class="ch.qos.logback.classic.net.SocketAppender">-->
<!--        <file>logs/app.log</file>-->
<!--        <append>true</append>-->
<!--        <encoder>-->
<!--            <pattern>%d{dd/MM/yyyy HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>-->
<!--        </encoder>-->
<!--    </appender>-->

    <springProfile name="dev">
        <root level="INFO">
            <appender-ref ref="console" />
        </root>

        <logger name="com.jp.thymeleaf.thegoodthymesvirtualgrocery" level="INFO" additivity="false">
            <appender-ref ref="console" /> <!-- passar o valor do atributo name da tag appender-->
        </logger>
    </springProfile>

    <springProfile name="prd">
        <root level="INFO">
            <appender-ref ref="file" /> <!-- passar o valor do atributo name da tag appender-->
        </root>

        <logger name="com.jp.thymeleaf.thegoodthymesvirtualgrocery" level="INFO" additivity="false">
            <appender-ref ref="file" /> <!-- passar o valor do atributo name da tag appender-->
        </logger>
    </springProfile>

</configuration>